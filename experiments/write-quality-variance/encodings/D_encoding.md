The bug being investigated concerns `xarray.where`, a top-level function defined at line 1730 of `xarray/core/computation.py` in the xarray library (working from the worktree at `experiments/phase2-utility/worktrees/bootstrap_where_keep_attrs_add`). The function signature is `def where(cond, x, y)` and it returns elements from `x` or `y` depending on the boolean condition `cond`, performing xarray-style broadcasting across the three arguments. The user-facing symptom is that `xr.where` does not preserve attributes (`.attrs`) from its inputs — there is no `keep_attrs` parameter on the function at all, so callers have no way to request that attrs be carried through to the output. This is inconsistent with `DataArray.where` and `Dataset.where` methods (mentioned in the See Also section at lines 1808-1809) and with the broader xarray convention where most computation functions accept a `keep_attrs` keyword.

The investigation began by locating the worktree (branch name `bootstrap_where_keep_attrs_add` strongly hints at the intended fix: adding `keep_attrs` to `where`). The agent listed the repo root to confirm this is the xarray codebase (CITATION.cff, xarray/, asv_bench/, properties/, conftest.py at top level — standard xarray layout). Then grep located the `where` definition at `xarray/core/computation.py:1730`. Reading lines 1725-1810 revealed the docstring and signature but the truncated read cut off before showing the implementation body starting around line 1810-1830, which is the critical region containing the `apply_ufunc` call that needs modification. A second read of lines 1804-1854 attempted to capture the implementation but the truncation jumped past it to line 1848 (the `polyval` function or similar), suggesting the `where` body is roughly lines 1810-1830 and was not fully captured in the transcript — a future agent should re-read lines 1810-1845 specifically.

Grepping `keep_attrs` across `computation.py` surfaced the pattern used elsewhere: line 32 imports `_get_keep_attrs` from `.options` alongside `OPTIONS`. The canonical idiom (visible at lines 1116-1117) is `if keep_attrs is None: keep_attrs = _get_keep_attrs(default=False)` — this resolves a `None` argument to the global xarray option. Other `keep_attrs` usages occur at lines 267, 281, 287, 302, 444, 458, 752, 1138, 1145, 1162, 1172, 1862, 1892, demonstrating that `keep_attrs` is passed through to `apply_ufunc` via the `keep_attrs=` kwarg (lines 1145, 1162, 1172) and also flows into `merge_attrs(..., combine_attrs=keep_attrs)` (lines 287, 302, 444, 458, 752) and `build_output_coords(..., combine_attrs=keep_attrs)`. Note the subtle dual semantics: `keep_attrs` accepts both legacy boolean values and the newer string values like `"override"`, `"drop"`, `"no_conflicts"`, etc., used as a `combine_attrs` strategy (line 281 special-cases the `"drop"` string).

The root cause at the code level is simply that `where` was written without plumbing `keep_attrs` into its internal `apply_ufunc` call. The implementation of `where` (around lines 1810-1830, not fully shown in transcript) calls `apply_ufunc(duck_array_ops.where, cond, x, y, ...)` and omits `keep_attrs`, so `apply_ufunc` defaults to dropping attributes.

The fix is to add a `keep_attrs=None` parameter to the `where` signature at line 1730, resolve `None` via `_get_keep_attrs(default=False)` at the top of the body (mirroring lines 1116-1117), and pass `keep_attrs=keep_attrs` into the `apply_ufunc` call inside `where`. The docstring (lines 1731-1810) should also gain a `keep_attrs` parameter description. Because xarray accepts both boolean and string forms, the parameter should be typed as `bool = None` for consistency with line 1862's `keep_attrs: bool = None` on the neighboring `_calc_idxminmax` helper, though `apply_ufunc` itself accepts the broader callable/string forms.

Alternative approaches considered: (1) handling attrs manually in `where` by reaching into `cond.attrs`/`x.attrs` after the `apply_ufunc` call and merging via `merge_attrs` directly — rejected because it duplicates logic that `apply_ufunc` already implements correctly via `combine_attrs`, and would diverge from how every other function in this file delegates attr handling. (2) Always preserving attrs unconditionally — rejected because it breaks backward compatibility and contradicts the project-wide opt-in convention controlled by the `keep_attrs` global option. (3) Defaulting `keep_attrs=True` rather than `None` — rejected because `None` correctly defers to the user's global `OPTIONS["keep_attrs"]` setting via `_get_keep_attrs`, which is the established pattern at line 1116.

Non-obvious discoveries: `_get_keep_attrs` is imported from `xarray/core/options.py` (line 32) and is the standard resolver — never read `OPTIONS["keep_attrs"]` directly. The `combine_attrs` parameter to `merge_attrs` and `build_output_coords` accepts the same vocabulary as `keep_attrs` (strings like `"override"`, `"drop"`, `"no_conflicts"`, `"identical"`, `"drop_conflicts"`), which is why `keep_attrs` can be passed through transparently. The function lives in `xarray/core/computation.py` alongside `apply_ufunc`, `dot`, `polyval`, and `_calc_idxminmax` — all the top-level computation helpers. The transcript's truncated reads mean a future agent should explicitly re-read the body of `where` (lines 1810-1845) before patching, and should add a corresponding test in `xarray/tests/test_computation.py` covering `xr.where(cond, x, y, keep_attrs=True)` with attrs on each of the three inputs.