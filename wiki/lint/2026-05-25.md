# Wiki Lint Report — 2026-05-25

**Run by:** Nils (indigo)
**Trigger:** post-`/wiki-ingest` of the T_A1b de-risk run results (REJECTED).
**Summary:** 28 findings, **0 errors**. The ingest introduced 5 findings (1 status-mismatch + 4 missing-construct-validity), all fixed in-session. The remaining 28 are pre-existing and unrelated to this ingest: tagged-page-needs-sources=2, dangling-link-pending=13 (info), missing-construct-validity=13.

## Findings requiring action

None blocking. All pre-existing, deferrable to a dedicated cleanup pass (per [feedback_discipline_sustainability] — not worth grinding mid-ingest):

- **tagged-page-needs-sources (2, warn):** [decision/caddy-as-research-program](../decision/caddy-as-research-program.md), [decision/research-backlog-stack-rank](../decision/research-backlog-stack-rank.md) — both carry `[ASSERTED]` tags with empty `sources:` frontmatter. Fix = walk inline citations, populate frontmatter. Pre-existing.
- **missing-construct-validity (13, warn):** all on older experiment pages — probe-2-phase-3-results, probe-2-phase-4-results, probe-2-phase-4-arc-results, training-corpus-selection, plus ~6 on concept/decision pages. These `[MEASURED]` tags predate this session (this ingest only repointed link paths in the probe-2 pages, did not touch their tags).
- **dangling-link-pending (13, info):** known-future `"pending"` targets (source pages not yet written: minference, lanham-2023-cot, shinn-2023-reflexion, etc.). Intentional placeholders, not bugs.

## Findings fixed this session (introduced by the ingest)

- **status-mismatch** on [experiment README](../experiment/2026-05-18-T_A1b-isolation-derisk/README.md) — LANDED experiment lacked a Results section. Fixed: added a `## Results` section pointing to derisk-run-results.md.
- **missing-construct-validity ×4** — derisk-run-results.md (×2), H44 (×1), masked-vs-forward-prediction (×1). Fixed: dropped a bare `[MEASURED]` on a provenance sentence; added construct-validity notes adjacent to each measured claim (the H44 note initially landed just outside the ±400-char window and was moved next to the tag).

## Contradiction sweep (rule 4)

Checked the new content against the tagged-claim inventory. No contradictions: the de-risk's `[MEASURED]` claims (arc 19.2%, skill +1.1%/+26.7%, effective rank 2.5/7/346.8) are novel measurements with no prior tagged value elsewhere, and H44's status flip (PROPOSED→REJECTED) has no competing page asserting SUPPORTED. The 939-entry full inventory was not exhaustively re-swept (unchanged since the 2026-05-20 pass).

## Drift to flag

- The ±400-char window for rule 8 (missing-construct-validity) bites when a claim sentence contains a long inline markdown link before the construct-validity note (the H44 case): the link path consumed the budget. Minor authoring tell — put the CV note adjacent to the tag, not after the evidence link. Not worth a rule change.

## Raw linter output

`28 findings — tagged-page-needs-sources=2, dangling-link-pending=13, missing-construct-validity=13. 0 errors.`
