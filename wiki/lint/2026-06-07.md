# Wiki Lint Report — 2026-06-07

**Run by:** Nils (indigo)
**Wiki state:** ~145 content pages + root; post-MASQ-ingest (5 new pages, 4 updated)
**Summary:** 37 findings after fix (was 38). 1 error (steady-state), 3 tagged-page-needs-sources (steady-state trio), 13 dangling-link-pending (info, steady-state), 20 missing-construct-validity (warn, all pre-existing). **Zero session-introduced findings remain** — the one new finding (my concept page flagged empty-sources) was a parser limitation and is fixed. No rule-4 contradictions.

## Findings requiring action

None outstanding. One session-introduced finding found and fixed in-session:

- `concept/multi-party-attribution-gap.md` + `decision/masq-paper-as-active-program.md` — flagged (concept) / latent (decision) `tagged-page-needs-sources`. **Root cause:** I authored `sources:` in block-list YAML form; the linter's permissive frontmatter parser (lint.py:91-92) only reads the inline `sources: [...]` form and treats block lists as empty. **Fix:** converted both to inline form. Concept page now passes; rule-3 count dropped 4→3.

## Addendum — trio cleared + parser fixed (same session, after AJ go-ahead)

After the initial report, two follow-on actions landed:

1. **SCHEMA v0.7.1 — parser tolerance fix.** `parse_frontmatter` now reads block-list YAML, not just inline `sources: [...]`. Genuinely-empty `sources:` still reads falsy. (The empty-sources flag on `multi-party-attribution-gap.md` was the trigger; fixed by converting to inline + this parser fix.)
2. **The 3 steady-state tagged-page-needs-sources findings cleared honestly** (no fabricated provenance):
   - `open-question/tier-3-structural-vs-semantic.md` → `sources: [../experiment/.../ceiling-probe.md]` (the `[MEASURED]` BoW-82% genuinely derives from it).
   - `decision/research-backlog-stack-rank.md` → `sources: [../experiment/.../README.md]` (cited inline; descriptive roll-up; SUPERSEDED chronicle).
   - `decision/caddy-as-research-program.md` → had **no** `source/*`/`experiment/*` citation; its lone `[ASSERTED]` tagged the strategic decision statement itself (a choice attributed in-prose to AJ + Eira + advisors, not an empirical claim). **Dropped the mis-scoped tag** rather than invent a source — per [internal-synthesis ≠ external-evidence]. Decision content unchanged.

Post-action full-lint count: **34** (0 errors actionable beyond the pre-existing ARS dangling-link; 0 tagged-page-needs-sources; 13 pending-link info; 20 pre-existing missing-CV). The findings below are the original report's adjudications, retained for the record.

## Findings adjudicated as accepted (steady-state, not this session)

- **1 error — `source/ars-academic-research-skills.md:109`** dangling link to `papers`. Carried since 2026-05-27; ARS-provenance page, low priority. Unchanged.
- **3 warn — tagged-page-needs-sources:** `decision/caddy-as-research-program.md`, `decision/research-backlog-stack-rank.md`, `open-question/tier-3-structural-vs-semantic.md`. The trio carried steady-state since 2026-05-27 (noted in the 2026-05-30 report). Real but deferred — these pages cite inline but haven't had `sources:` frontmatter populated.
- **13 info — dangling-link-pending:** all `[pending]`-marked forward references to unwritten source pages (Lanham 2023, Shinn 2023, MInference, Reflexion, etc.). By-design markers, not bugs. Unchanged from prior runs.
- **20 warn — missing-construct-validity:** all on pre-existing pages (consolidation-channel, the T_A1b probe-2 experiment series, source pages quoting cited [MEASURED] sub-results). None on the five new pages — my [MEASURED] tags (concept Evidence section; decision feasibility claims) carry inline construct-validity notes within the ±400-char window. Steady-state.

## Contradiction sweep (rule 4)

No genuine contradictions. One adjacency inspected and cleared:

- **LongMemEval degradation numbers.** `open-question/multiplicativity-vs-overlap.md:20` and `concept/benchmark-replication-gap.md` carry `[MEASURED]` "49% recall on LongMemEval (independent third-party re-run)" — this is *Mem0's* recall in an independent replication. New `source/wu-2024-longmemeval.md` carries the paper's "GPT-4o Oracle→full 30.3% drop." Different systems (a vendor memory system vs a base LLM), different metrics (recall vs oracle-to-full accuracy degradation). Not the same claim → different framings are correct. No tag collision.
- New gap claim (`concept/multi-party-attribution-gap.md`) vs `open-question/billion-scale-benchmark-gap.md`: reconciled in-session — the billion-scale page's MASQ sub-question now explicitly states MASQ does *not* close the billion-scale gap (it's a conversational-scale attribution benchmark). Consistent, not contradictory.

## Drift to flag

- **Parser/author-convention mismatch (worth a one-line SCHEMA note).** The linter parses only inline `sources: [...]`; block-list YAML `sources:\n  - x` silently reads as empty and produces a false `tagged-page-needs-sources` warning. Both forms are valid YAML and Obsidian renders both. Either (a) document "use inline form" in SCHEMA's frontmatter conventions, or (b) extend `parse_frontmatter` to accumulate `-`-prefixed continuation lines into the current key (the parser currently `continue`s past them at line 91-92). Low urgency — convention (a) is a one-line fix; (b) is the more robust fix. Flagging for AJ since parser changes touch shared tooling.

## Raw linter output

Full 38-finding pre-fix report elided for brevity; re-runnable via `python3 lint.py --include-inventory`. Post-fix rule-3 count = 3 (steady-state trio).
