# Wiki Lint Report — 2026-05-20

**Run by:** Nils (indigo)
**Wiki state:** 209 pages at new shared path `wiki/` (post-restructure)
**Summary:** First lint after Locke's wiki move (`wiki/` → `wiki/`). Started at 22 findings — 7 dangling-link errors, 15 dangling-link-pending info. All 7 errors fixed in-session. Final state: 0 errors, 15 info (all intentional pending references). One tooling fix made: `lint.py` had a hardcoded path to the old wiki location.

## Findings requiring action

**All cleared in-session.** The 7 dangling-link errors were post-move artifacts from Locke's path-rewrite pass, in two patterns:

### Pattern 1: cross-mount relative path (1 error)

- `concept/evidence-anchoring.md:77` — link target `../../../.claude/skills/wiki-lint/` could resolve from the *old* wiki path (`wiki/` → `...`) but cannot resolve from the new shared location (`wiki/` cannot reach `/workspace/` via relative path; different bind-mount). **Fix:** converted to absolute path `wiki-lint`. Container-internal — not portable across machines, but at least correct in the indigo container where the lint script lives. Flagged as future drift; the skill itself probably needs a path-discovery layer or relocation if it's to be invoked from other containers' workspaces.

### Pattern 2: missing `..` after extra `wiki/research/` depth (6 errors)

Locke's rewrite correctly retargeted `experiments/*` to `research/experiments/*` (sibling folder under the new shared root), but didn't add the extra `..` needed because the wiki now lives one level deeper inside `wiki/research/`. Affected files:

- `experiment/2026-05-11-write-quality-variance/exp1-task2.md:28` — spec file ref
- `experiment/2026-05-11-write-quality-variance/exp1-task2.md:29` — results dir ref
- `experiment/2026-05-11-write-quality-variance/exp1-task2.md:60` — raw artifacts ref
- `experiment/2026-05-11-write-quality-variance/exp2-task3-c-replication.md:22` — spec file ref
- `experiment/2026-05-11-write-quality-variance/exp2-task3-c-replication.md:23` — results dir ref
- `experiment/2026-05-11-write-quality-variance/exp2-task3-c-replication.md:55` — raw artifacts ref

**Fix:** added one more `../` to each (`../../../research/...` → `../../../../research/...`). Diagnosis pattern is mechanical and uniform across all six.

## Findings adjudicated as accepted

**15 dangling-link-pending (info-severity).** All marked with `"pending"` link-title attribute, the documented mechanism for intentional forward references to not-yet-written pages or to agent-memory files. Spot-checked the cases that looked suspicious (`../feedback_capability_vs_resource`, `../../_archive/reference_paper_repo`); confirmed both are intentional references to my agent-memory store, surfaced via the pending mechanism. No action needed.

## Contradiction sweep (rule 4)

**Scope:** post-move sanity pass, not a deep audit. Spot-checked the recently-active change areas (m17-jepa-reconciliation RESOLVED 2026-05-19, k2-ta3-deferred-to-v2 same-day demotion, T_A1b becoming sole MVP T4). Walked the 117 `[MEASURED]` entries quickly for obvious cross-page collisions on the same numeric claim; did not exhaustively walk the 630 `[ASSERTED]` entries — deferred to `/wiki-audit` per skill scope.

### Latent ambiguity — adjudicated NOT a contradiction

`open-question/m17-jepa-reconciliation.md:13` (resolution headline) says K2+T_A3 "remains a load-bearing T4 alongside T_A1b". `decision/k2-ta3-deferred-to-v2.md:12` says K2+T_A3 is "deferred from MVP load-bearing T4 status to v2 research target" and T_A1b is "sole load-bearing T4 on the MVP critical path". **Verdict:** not a contradiction — m17 line 19 explicitly disambiguates: "scientifically distinct" (line 13's sense, with its own falsifiable hypothesis H40) vs "MVP critical-path status" (line 12 on k2-ta3-deferred-to-v2's sense, deferred because the wedge demo doesn't require it). This is exactly the two-axis distinction in agent-memory `[Scientific vs MVP Load-Bearing]` (2026-05-19 YAGNI test).

**Future-agent risk** (not action-required): line 13 in isolation is ambiguous — an agent grabbing just the resolution line could miss the disambiguation on line 19. A tighter wording ("scientifically load-bearing T4") would be more robust, but the current text is correct in context. Logged here so AJ can decide whether to tighten.

## Soft claims accepted (rule 7)

Retired in v0.4 — no findings.

## Drift to flag

**`lint.py` hardcoded the old wiki path.** `DEFAULT_WIKI = Path("wiki")` survived the restructure; Locke updated the SKILL.md instructions but not the script. First lint run after the move silently inspected the stale `kyrja` copy and reported only its single dangling-link error, masking the 6 new ones the move introduced into the new copy. **Fixed in-session** — `DEFAULT_WIKI` now points at `wiki/`. Lesson for the move-cleanup checklist: tooling that targets the wiki by path needs the same sweep as the wiki content itself; running `/wiki-lint` post-move was the right verification step and surfaced this gap correctly once the path was right.

**Two stale wiki copies during transition.** Indigo container still has `wiki/` (the original local copy, == `wiki` from host view). Per Locke's note this stays until I'm satisfied with the move; deleting it is the natural end-of-transition step.

## Raw linter output

Final state after fixes:

```
# Wiki Lint Report — 2026-05-20
**Summary:** 15 findings — dangling-link-pending=15
(0 errors, 0 warnings; 15 info-severity intentional pending references)
```
