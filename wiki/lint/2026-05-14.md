# Wiki Lint Report — 2026-05-14

**Run by:** Nils (indigo)
**Wiki state:** ~85 pages (concept/decision/experiment/hypothesis/source/incumbent/open-question + root nav)
**Summary:** 11 findings — 0 errors, 3 stale-source warnings, 8 dangling-link-pending (info). All pending-links are intentional. Stale-source warnings are pre-existing residual from this morning's EvoSC + Hope ingest, not from today's H38 ingest. No rule-4 contradictions surfaced on the H38-relevant sweep.

## Findings requiring action

None of severity `error`. The three `warn`-severity findings below are real but adjudicated as **defer, not fix** — see "Findings adjudicated as accepted" below for why bumping dates without re-reviewing would hide drift rather than resolve it.

| File | Severity | Finding |
|---|---|---|
| `hypothesis/H29-edge-substrate-memory.md:53` | warn | stale-source — cites `source/xu-2026-agentic-memo.md` (target last_ingested 2026-05-14 > citing 2026-05-13) |
| `open-question/procedural-memory-alternative.md:50` | warn | stale-source — same cited source, same gap |
| `source/sun-2024-ttt.md:73` | warn | stale-source — same cited source, same gap |

**Proposed disposition.** Defer to the next time any of the three citing pages is materially edited; verify citations against the updated `xu-2026-agentic-memo.md` text at that point and bump `last_ingested`. The xu-2026-agentic-memo.md edit was an "expanded Nested Learning entry in high-priority follow-up reads" (per the 2026-05-14 Hope ingest log) — additive, not retractive, so existing citations are unlikely to have been invalidated. Acceptable to carry the warning.

If AJ prefers a clean lint output today, the alternative is a quick verify-and-bump pass on these three pages (~5 minutes total). Mechanical date-bump alone would be wrong — it'd silence the warning without doing the review the warning is asking for.

## Findings adjudicated as accepted

**8 × dangling-link-pending (info-severity, all intentional):**

- `concept/consolidation-channel.md:15` → `./complementary-learning-systems.md` — pre-existing pending link; McClelland 1995 unread.
- `concept/substrate-as-memory.md:23` → `./sample-complexity-separation.md` — pre-existing pending link; standalone treatment of Xu Theorem 1.
- `hypothesis/H38-rationale-trace-memory.md:21,29,64` → `../source/shinn-2023-reflexion.md` — 3 instances of the same target. Per `feedback_load_bearing_sources`, source page deferred until Reflexion is read verbatim. Pending markers are correct.
- `hypothesis/H38-rationale-trace-memory.md:22,36,64` → `../source/lanham-2023-cot-faithfulness.md` — 3 instances of the same target. Same discipline; deferred until verbatim read. This is the load-bearing falsifier for H38, so promotion-to-source should happen before H38 advances past PROPOSED.

## Contradiction sweep (rule 4)

Reviewed the H38-relevant slice of the 293-entry inventory for collisions with existing tagged claims. No contradictions found:

- **CoT faithfulness** — `[ASSERTED]` in H38; not previously tagged elsewhere in the wiki. No collision.
- **Reflexion as prior art** — `[ASSERTED]` in H38; not previously tagged elsewhere. No collision.
- **Xu et al. 2026 CSC theorem** — H38 cites as `[ASSERTED]` (consistent with existing usage across `consolidation-channel.md`, `substrate-as-memory.md`, `H29`, `H37`). Tag consistency holds.
- **Active-stages framework loop-level applicability** — H38's `[SPECULATED]` framing on loop-level approximation is consistent with `active-stages-framework.md`'s own `[SPECULATED]` framing on the framework's design recommendations.

Not a full 293-entry sweep — focused review on the H38-touched surface. Full sweep is a `/wiki-audit`-scoped activity, deferred.

## Soft claims accepted (rule 7)

Rule 7 is retired in schema v0.4. Nothing to adjudicate here. Authoring discipline + future `/wiki-audit` carry the work.

## Drift to flag

**None for schema discipline.** Lint behaviour matches expectations; no rule is producing systematic false positives at this scale.

**One observation worth recording for the next ingest:** the `[pending]` marker convention is doing real work — without it, H38's 6 source-page-pending references would have been errors rather than info-level. The convention is paying for itself in this kind of "hypothesis lands before sources are read verbatim" pattern, which is exactly the load-bearing-sources discipline applied at the wiki layer.

## Raw linter output

11 findings — stale-source=3, dangling-link-pending=8. See `python3 lint.py --include-inventory` for full inventory (293 tagged-claim entries).
