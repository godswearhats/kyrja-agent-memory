# Wiki Lint Report — 2026-05-30

**Run by:** Nils (indigo)
**Wiki state:** ~140 content pages + root files; run after the 2026-05-30 strategy-exploration ingest (1 page created, 6 updated).
**Summary:** 39 findings at first run — 1 error, 16 warn, 22 info. **Zero session-introduced.** The new page (`thinks-with-wedge-sizing.md`) is clean (its market-data `[MEASURED]` carries an inline construct-validity note). Two missing-CV warnings on pages I edited today were *pre-existing* `[MEASURED]` summary lines from the un-linted 2026-05-28 ingest — fixed in-session (22→20 missing-CV). Everything else is the known steady-state backlog carried from 2026-05-27.

## Findings requiring action

1. **`error` — `source/ars-academic-research-skills.md:109`** — dangling link to `papers` (target does not exist). **Pre-existing** (flagged in the 2026-05-27 report, not yet repaired). Fix: repoint to the correct library path or remove the link. Not session-related; recommend a dedicated cleanup touch. *(Carried, not introduced.)*

## Findings adjudicated as accepted

- **tagged-page-needs-sources (3, warn)** — `decision/caddy-as-research-program.md`, `decision/research-backlog-stack-rank.md`, `open-question/tier-3-structural-vs-semantic.md`. Pre-existing; these declare empty `sources:` frontmatter. Steady-state backlog (same three across prior reports). Populate during a dedicated source-sweep, not tonight.
- **dangling-link-pending (13, info)** — pending `source/*` pages not yet created (Reflexion, CoT-faithfulness, MInference, ColBERT/REALM, SPW-R biomarker, etc.). Intentional `[pending]` markers; not errors.
- **missing-construct-validity (20 remaining, warn)** — after tonight's 2 fixes. Breakdown: ~16 on the older `2026-05-18-T_A1b-isolation-derisk/*` probe pages (steady-state since 2026-05-27), plus `phase2-results.md` (×2), `spec.md`, `consolidation-channel.md:72`, `scaling-and-memory-gates.md:29`, `H44`. The `scaling-and-memory-gates.md:29` hit is a **rule-window artifact** — the page has a thorough `### Construct-validity note`, just in its own subsection >400 chars from the `## Results` `[MEASURED]` preamble. Accepted, not a real gap.

## Fixes applied this session

- **`concept/integration-gate.md:41`** — added an inline construct-validity clause to the Gate-2 `[MEASURED]` summary line (train-loss diagnostic). Cleared.
- **`open-question/incremental-integration-cost.md:47`** — same, on the parallel summary line. Cleared.

Both pages already at `last_ingested: 2026-05-30`.

## Contradiction sweep (rule 4)

Focused sweep over today's new/changed claims against the 1001-entry inventory — **no contradictions introduced.** The market-data figures (new-operands ≈ p90; coding $4.0B/55%; etc.) are novel to the wiki and collide with nothing. The Gate-1 numbers (integrated 0.523/0.500) and the Gate-2 failure (0.347, retention 0.500) are stated consistently across `scaling-and-memory-gates`, `integration-gate`, and `incremental-integration-cost`. Full 1001-entry adjudication is out of scope for routine lint (→ future `/wiki-audit`).

## Drift to flag

- **Rule-8 window vs sectioned CV notes.** `scaling-and-memory-gates.md:29` and similar experiment pages keep a dedicated `### Construct-validity note` subsection (good authoring practice) but the `[MEASURED]` preamble under `## Results` sits >400 chars away, so the rule fires a false positive. Worth considering: let rule 8 treat a `### Construct-validity` subsection anywhere under the same `##` parent as satisfying the check. Schema-discipline call for AJ; not urgent.
- The 2026-05-28 ingest landed un-linted (no report between 05-27 and tonight), which is why two summary-line CV gaps surfaced late. Minor; the gates caught them tonight.

## Raw linter output

First run: 39 findings — tagged-page-needs-sources=3 (warn), dangling-link=1 (error), dangling-link-pending=13 (info), missing-construct-validity=22 (warn). Post-fix: missing-construct-validity=20.
