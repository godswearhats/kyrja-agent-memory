# Wiki Lint Report — 2026-05-27

**Run by:** Nils (indigo)
**Wiki state:** research wiki; 33 findings (1 session-introduced → fixed in-session; 32 pre-existing/steady-state).
**Summary:** Post-ingest lint after the factored-operator beachhead ingest (spec + phase1-results + propagation). Exactly **one** finding was introduced this session — a stray `[MEASURED]` tag on a Method-section description in `phase1-results.md` — fixed in-session. The remaining 32 are pre-existing and already characterised in prior reports: 3 tagged-page-needs-sources, 1 dangling-link error, 13 dangling-link-pending (info), 15 missing-construct-validity on older T_A1b pages. No rule-4 contradictions introduced.

## Findings requiring action

**Fixed in-session (session-introduced):**
- `experiment/2026-05-26-factored-operator-beachhead/phase1-results.md:20` — *missing-construct-validity*. Root cause: a `[MEASURED]` tag on the Method-section intro ("CPU-only … stand-ins, not models"), which is procedural description, not a measured claim. **Fix applied:** removed the stray tag. The genuine `[MEASURED]` results (Results section + table) carry their note in the `### Construct-validity note` subsection.

**Pre-existing, deferred (not this session's — flag to AJ):**
- `source/ars-academic-research-skills.md:109` (**ERROR**) — dangling link to `papers` (dir absent). From the 2026-05-26 ARS ingest; correct target unknown to me, so flagging rather than guessing.
- 3× *tagged-page-needs-sources* — `decision/caddy-as-research-program`, `decision/research-backlog-stack-rank`, `open-question/tier-3-structural-vs-semantic` (empty `sources:` frontmatter).
- 15× *missing-construct-validity* on `experiment/2026-05-18-T_A1b-isolation-derisk/*` and `hypothesis/H44` — pre-existing; deferred to a dedicated cleanup per `feedback_discipline_sustainability`.
- 13× *dangling-link-pending* (info) — intentional forward-reference stubs.

## Contradiction sweep (rule 4)

No contradictions introduced. The new `[MEASURED]` claims (`phase1-results`) describe a brand-new idealised validation with no counterpart claim elsewhere; the `integration-gate` `[ASSERTED]/[SPECULATED]` claims concern the *general* composition bet (a different proposition). No same-claim/different-tag collisions found against the new pages.

## Drift to flag

None new. The 13 pending links + 15 older missing-CV have persisted across several reports — known backlog, not fresh drift; no schema change indicated. The `missing-construct-validity` rule correctly fired on the Method-section false-positive (a genuine authoring slip), so no rule-tuning needed.

## Raw linter output

33 findings: tagged-page-needs-sources=3, dangling-link=1 (error), dangling-link-pending=13, missing-construct-validity=16 (→15 after the in-session fix). Generated by `lint.py --include-inventory`, 2026-05-27.
