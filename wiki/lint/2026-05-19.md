---
tags:
  - exclude-from-graph
---
# Wiki Lint Report — 2026-05-19

**Run by:** Nils (indigo)
**Wiki state:** ~172 pages incl. archive (6 new pages + 9 propagation updates from 2026-05-18 ingest landed since last lint)
**Summary:** Initial run produced 18 findings (3 warn + 15 info). All 3 warnings fixed in-session by re-tagging mis-applied `[ASSERTED]` on decision/concept pages and adding the corresponding source-frontmatter entries. Final state: **15 findings, all info-severity pending-link markers (intentional). Zero errors, zero warnings.**

## Findings requiring action

None remaining after in-session fixes.

## Findings fixed in-session

Three `tagged-page-needs-sources` warnings on pages created during the 2026-05-18 ingest. Root cause: I applied `[ASSERTED]` to the page's central decision/definition statements, which is mis-use of the tag per SCHEMA. `[ASSERTED]` means "claimed, not measured — *borrowed from a paper or competitor*." Decision and definition statements are *our own commitments*, not borrowed external claims — the page type itself is the epistemic claim.

Fix shape on each page: removed `[ASSERTED]` from the decision/definition statement; kept `[ASSERTED]` only on genuinely-external claims with inline source links; populated `sources:` frontmatter with the source pages those inline links actually point to.

- **[decision/tier-3-4-as-wedge.md](../decision/tier-3-4-as-wedge.md)** — removed `[ASSERTED]` from the Decision statement; kept `[ASSERTED]` on the post-Norman-gap claim (which is genuinely external); added inline `[Dong/Norman 2025](../source/dong-2025-norman-episodic.md)` link; populated `sources:` with the Norman source page.
- **[decision/multi-field-memory-unit.md](../decision/multi-field-memory-unit.md)** — removed `[ASSERTED]` from the Decision statement; kept `[ASSERTED]` on the "MemTx is hybrid by accident" claim (genuinely citing MemTx §4); added inline `[Wu 2022 §4](../source/wu-2022-memorizing-transformer.md)` link; populated `sources:` with the MemTx source page.
- **[concept/memory-retrieval-tiers.md](../concept/memory-retrieval-tiers.md)** — removed `[ASSERTED]` from the Definition statement; kept `[ASSERTED]` on the post-Norman-gap claim; added inline Norman link; populated `sources:` with the Norman source page.

Verified by re-running the linter: warnings cleared.

## Findings adjudicated as accepted

15 `dangling-link-pending` findings, all info-severity. Every one carries the `"pending"` title attribute per SCHEMA's pending-link convention. These are known-future targets, not bugs:

- 2 in `concept/retrieval-granularity.md` (memory + archive paths)
- 1 in `concept/substrate-as-memory.md` (sample-complexity-separation concept)
- 1 in `hypothesis/H37-pluggable-substrate.md` (Jiang 2024 MInference)
- 6 in `hypothesis/H38-rationale-trace-memory.md` (Lanham 2023 + Shinn 2023, multiple references)
- 2 in `hypothesis/H43-soft-composition-emergent-construction.md` (REALM + ColBERT pending sources)
- 2 in `hypothesis/H44-T_A1b-cross-domain-transfer.md` (Bardes 2024 V-JEPA + Ge 2024 ICAE — pending source pages flagged during today's ingest)
- 1 in `source/mcclelland-mcnaughton-oreilly-1995-cls.md` (Buzsáki SPW-R cognitive-biomarker page)

The H44 pending sources are particularly worth noting — they're the load-bearing external precedents for the wedge bet. Promotion to real `source/*` pages would be a natural next step when the de-risk experiment build begins (when those papers get verbatim reads).

## Contradiction sweep (rule 4)

Spot-checked the highest-collision-risk claims in the 875-entry inventory:

- **EM-LLM 2.5/5 Norman-rubric ceiling.** Cited consistently as `[ASSERTED]` across `index.md`, `H44`, `norman-rubric.md`, `caddy-architecture.md`, `memory-retrieval-tiers.md`, `spens-2024-hippocampal-rag.md`. Canonical source is `concept/norman-rubric.md` which catalogues the scoring. No contradiction.
- **ICAE auxiliary-loss-helps at context-compression scale.** Cited consistently across `H44`, `NOW.md`, `caddy.md`, `caddy-architecture.md`, `caddy-vs-bolt-on.md`. All frame it as "ICAE shows yes at context-compression scale; bet is whether result transfers to experiential-memory scale." No contradiction.
- **post-Norman gap (11 months, no system above 2.5/5).** Cited consistently across multiple pages with the Dong/Norman 2025 source.

Light sweep only — full inventory walk (875 entries) is judged not load-bearing given the new pages' claims all funnel into a small canonical-source set. Reserve a deeper sweep for a future `/wiki-audit`-style pass.

## Drift to flag

**Recurrence of "[ASSERTED] on Kyrja-internal decision" mis-tagging.** This is now the second ingest where I (Nils) applied `[ASSERTED]` to a decision page's own decision statement and triggered rule-3. The pattern: I'm using `[ASSERTED]` as "this is a committed claim" rather than the schema's narrower meaning ("borrowed from external paper or competitor"). Per [[feedback_internal_synthesis_evidence]] this is exactly the Cat 1/2/3 categorization issue — internal synthesis mis-tagging as external evidence.

The mechanical fix (this report) is fast. The discipline fix is authoring-side: at ingest-write time, ask *"is this claim borrowed from outside the wiki, or is it our commitment?"* before reaching for `[ASSERTED]`. Worth strengthening in the `/wiki-ingest` skill's "Discipline reminders" if it recurs a third time.

No schema-rule changes implicated. Rule 3 fired correctly both times.

## Raw linter output

Pre-fix, 18 findings: tagged-page-needs-sources=3 (the three fixed above), dangling-link-pending=15 (accepted as intentional pending markers).

Post-fix, 15 findings: dangling-link-pending=15 only. Zero errors, zero warnings.
