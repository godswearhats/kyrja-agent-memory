# Wiki Lint Report — 2026-06-30

**Run by:** Nils (indigo)
**Wiki state:** full tree; 44 findings on first pass, **0 error-severity** (exit 0).
**Summary:** Lint run after the MASQ retrieval-arms ingest (3 new pages + 3 edited pages). All findings were warnings/info — no dangling links, no status mismatches, no source-page-without-citation. 7 `missing-construct-validity` warnings landed on the pages I authored this session; all 7 fixed in-session (proximity edits placing the construct-validity keyword within ±400 chars of each `[MEASURED]` tag) and re-verified clean. The remaining 24 construct-validity warnings and 13 `dangling-link-pending` info findings are pre-existing on untouched pages — accepted as out of scope for this run.

## Findings requiring action

None outstanding. The 7 in-scope findings were fixed this session:

- `experiment/2026-06-30-masq-retrieval-arms.md` (4 tags: ladder, paired, diagnostic, size-sweep) — added a section-level construct-validity pointer + inline `*Construct-validity:*` notes on the paired and diagnostic blocks. Re-verified clean.
- `hypothesis/H45-exclusion-over-recall.md:38` (method-equivalence bullet) — added an inline construct-validity note (CI-overlap at n=15, not a proven null).
- `hypothesis/H33-routing-matters.md:31` — keyword was too far from the tag at bullet start; added a near-tag pointer and renamed the end-of-bullet `*Scope:*` → `*Construct-validity / scope:*`.
- `decision/structured-filter-first.md:26` — added a near-tag `*(construct-validity-limited — scope caveat at bullet end)*` pointer.

## Findings adjudicated as accepted

- **24 × `missing-construct-validity` (warn)** on untouched pages (T_A1b de-risk experiment set, factored-operator-beachhead, masq corpus-audit/gate1/bareset, H44, several concept pages). Pre-existing; not in this session's scope. Most are the established "one consolidated construct-validity note per multi-result experiment page" pattern that the ±400-char rule flags per-tag. Candidate for a future `/wiki-audit` pass, not routine lint repair.
- **13 × `dangling-link-pending` (info)** — intentional `pending`-marked forward links to not-yet-written source pages (Reflexion, CoT-faithfulness, MInference, ReALM, ColBERT, etc.). Correctly downgraded to info by rule 9. No action.

## Contradiction sweep (rule 4)

No genuine contradiction introduced or found among touched claims.

- **"bm25 ≈ vector ≈ hybrid / retrieval method barely matters"** — appears as `[MEASURED]` in [Thread 5 README](../experiment/2026-04-17-thread5-retrieval-comparison/README.md), [structured-filter-first](../decision/structured-filter-first.md), and the new [MASQ experiment](../experiment/2026-06-30-masq-retrieval-arms.md) / [H45](../hypothesis/H45-exclusion-over-recall.md) / [H33](../hypothesis/H33-routing-matters.md). **Consistent tag across all (MEASURED), consistent direction → independent replication, not collision.** This is the intended convergence; flagged here only to record it was checked.
- The MASQ "structured filter beats ranking" result (`[MEASURED]`, construct-limited) does not collide with `structured-filter-first`'s commitments (untagged design commitments) or with the embedding-anisotropy concept pages (consistent mechanism). No tag conflict.

## Soft claims accepted (rule 7)

Rule 7 retired in v0.4 — not run.

## Drift to flag

- **`missing-construct-validity` per-tag on consolidated-note experiment pages.** The ±400-char rule fires on every `[MEASURED]` tag whose construct-validity note lives in a single dedicated section below (the common experiment-page shape). This produced 4 warnings on a page that *does* carry a thorough construct-validity section. The fix that satisfies the rule (sprinkle the keyword near each tag) is mild redundancy. Not urgent, but if the count grows, consider: a page-level exemption when a section literally titled "Construct-validity" exists, OR treating an experiment page's dedicated note as covering all in-page `[MEASURED]` tags. Schema change → AJ conversation, per discipline. Noting, not acting.

## Raw linter output

44 findings: `dangling-link-pending`=13 (info), `missing-construct-validity`=31 (warn, 7 in-scope fixed → 24 pre-existing remain). 0 errors; exit 0 confirmed after fixes.
